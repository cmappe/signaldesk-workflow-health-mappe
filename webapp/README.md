# SignalDesk dashboard

Interactive web interface for the SignalDesk weekly health check.

## Run locally

Requires Node.js 22.13+ and pnpm.

```bash
pnpm install
pnpm run dev
```

Run `pnpm run build` to verify the production build.
